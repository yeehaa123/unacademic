import MainCtrl from '../MainCtrl';

class ConstellationCtrl extends MainCtrl {
  constructor(dispatcher, constellationResolver){
    super(dispatcher, constellationResolver);
  }
}

export default ConstellationCtrl;
